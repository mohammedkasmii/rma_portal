"""Phase 0 Ubuntu/Docker feasibility PoC for the OmegaFlow browser session.

Additive only: reuses ``rma_portal`` (session state, read-only request
policy, authentication detection, profile lock) and changes none of it.
"""
